/**
 * Barrel export for UI components
 * Provides a single import point for all reusable UI components
 */

export { default as ProjectCard, GithubIcon, type ProjectCardProps, type ProjectCardData } from "./ProjectCard";
export { Button } from "./button";
export { default as GridBackground } from "./GridBackground";
export type { ProjectFilter } from "@/types/project";
